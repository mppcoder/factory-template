# Отчет 2.6 runtime proof

Дата: 2026-04-27

## Статус

`runtime-proof-passed-infrastructure-image`

Approved runtime proof выполнен на VPS `72.56.26.209` в repo path `/projects/factory-template` с preset `production`.

Граница доказательства: это real VPS proof для production preset infrastructure path (`app-db`, `reverse-proxy-tls`, `backup`, `healthcheck`) с demo `APP_IMAGE=nginx:1.27-alpine`. Это не является proof реального application image.

## Выполненные gate'ы

- Pre-deploy env validation: pass, warning only for demo `APP_IMAGE`.
- Production dry-run: pass.
- Real deploy: pass.
- HTTPS healthcheck: pass for `https://72-56-26-209.sslip.io/`.
- Compose services: `app`, `db`, `reverse-proxy` healthy.
- Backup run: pass, dump `factory_template-latest.sql` created.
- Restore test: pass into disposable database `factory_template_restore_probe`, then dropped.
- Rollback drill: pass using local candidate tag `factory-template-rollback-candidate:local`, then restored previous image `nginx:1.27-alpine`.
- Static placeholder page: installed after proof to replace the default nginx page with generated repo-native placeholder content.

## Санитизированный transcript

No secrets, `.env` contents, passwords, tokens or private connection strings are included.

```text
pre_deploy_env_validation=pass warnings=APP_IMAGE demo nginx
production_dry_run=pass services=db,app,db-backup,reverse-proxy
deploy=pass services=db,app,reverse-proxy
https_healthcheck=pass bytes=615
backup=pass file=factory_template-latest.sql
restore_test=pass disposable_db=factory_template_restore_probe
rollback_drill=pass previous=nginx:1.27-alpine candidate=factory-template-rollback-candidate:local final_healthcheck_bytes=615
static_placeholder=installed url=https://72-56-26-209.sslip.io/ image_url=https://72-56-26-209.sslip.io/placeholder.svg svg_bytes=2433
```

## Дефекты, найденные во время runtime proof

- `reports/bugs/2026-04-27-runtime-env-user-burden-gap.md`: Codex initially pushed non-secret env setup to the user.
- `reports/bugs/2026-04-27-backup-hook-command-splitting.md`: backup hook command was split before shell execution.
- `reports/bugs/2026-04-27-skip-pull-defeated-by-pull-policy.md`: rollback `--skip-pull` was defeated by production `pull_policy: always`.
- `reports/bugs/2026-04-27-app-image-placeholder-burden-gap.md`: Codex should offer generated placeholder/static image URL before asking for a real app image.

## Оставшаяся граница

- Replace demo `APP_IMAGE=nginx:1.27-alpine` with a real application image before claiming application-level production proof.
- Keep `deploy/.env` and `.factory-runtime/` out of git.
