# Bounded runner / ограниченный runner

Generated project receives `scripts/bounded-task-runner.py` as a dry-run / one-task automation skeleton.

Current boundary: default max concurrency is `1`, no auto-merge, no production deploy, no public report publication, no security/external-secret/blocked tasks. Parallel execution stays disabled until worktree isolation and audit log are implemented and tested.
