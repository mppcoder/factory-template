# Ворота public submit

Public external report submission is default disabled.

Requires approval scope `public-submit`, redaction validator green, consent artifact, no secrets, no private URLs, no raw logs unless explicitly allowed and sanitized, target repo, audit ledger entry and rollback/update plan.

Dry-run creates a preview only; no live public submit is performed in verification.
