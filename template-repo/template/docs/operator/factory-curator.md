# Factory curator / куратор фабрики

Generated projects may use the factory curator docs as an operator contract, but the curator remains read-only by default.

Policy: no hidden self-learning; all learning becomes a repo-reviewed artifact; no mutation by default. Curator proposals must include evidence, proposal, risk and suggested validation before any human-approved task is created.
