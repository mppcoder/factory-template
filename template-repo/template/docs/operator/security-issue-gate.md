# Ворота security issue

Public security issue autofix refused.

Security work requires private report channel, approval scope `security-fix`, maintainer/admin actor, no public logs, no public issue body in handoff, sanitized private handoff, audit ledger without sensitive details, human review and separate auto-merge approval if merge is allowed by policy.
