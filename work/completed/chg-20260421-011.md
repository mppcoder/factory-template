# chg-20260421-011

## Заголовок
Move hot15 uploadables into one subfolder

## Что сделано
- `core_hot_direct` теперь явно фиксирует `upload_subdir: upload-to-sources`.
- Все uploadable файлы и `core-cold-5.tar.gz` экспортируются в `core-hot-15/upload-to-sources/`.
- Корень `core-hot-15/` оставлен только для `README.md` и `manifest.json`.
- Docs и boundary guidance обновлены так, чтобы пользователь открывал только одну подпапку и загружал всё её содержимое.

## Почему это важно
Теперь физическая структура папки сама показывает, что нужно выгружать в ChatGPT Project Sources, без marker-файлов и без смешения со служебными файлами.
