# chg-20260422-007

## Заголовок
Normalize canonical factory hierarchy and ship 2.4.4

## Что сделано
- canonical core/release-facing иерархия очищена от product-specific naming: `dogfood` и `openclaw` больше не выступают как core factory structure
- presets, workspace packs, optional domain references и examples переведены на универсальные factory names с compatibility aliases для legacy preset names
- versioning, release docs, manifests, template/runtime metadata и `.chatgpt` closeout artifacts синхронизированы под `2.4.4`

## Почему это важно
Теперь downstream-consumed release-facing контур описывает одну и ту же универсальную фабрику во всех слоях: docs, manifests, launcher/runtime, examples и optional references.
