# Пример brownfield reference-case проекта

Статус примера: scaffold-only

Этот пример показывает только каркас domain-specific reference-case и не обязан проходить полный набор валидаторов.

## Статус примера
scaffold-only

Этот пример показывает только стартовую структуру проекта.
Он не является эталоном полного прохождения валидаторов.
Для полного примера используйте:
- example-change-small-fix
- example-change-brownfield-audit
- example-change-end-to-end
