# Проверка реальности

## Текущее состояние проекта
- [PROJECT] Подпись кнопки хранится в файле `src/ui/notify_form.html`.
- [PROJECT] Ветка `release/2026.04` уже содержит исправления соседних текстовых дефектов, но эта строка не менялась.
- [PROJECT] В текущем шаблоне отображается опечатка `Сохранть`, которая подтверждена ручной проверкой формы.

## Что говорит документация
- [DOC] Внутренний гайд `docs/ui-copy.md` требует использовать корректные глагольные подписи для кнопок.
- [DOC] В release note версии `v2.14` указано, что текстовые правки интерфейса допускаются без миграции данных и без изменения API.

## Реальные кейсы и известные баги
- [REAL] Команда поддержки уже фиксировала похожие ошибки в подписях кнопок после срочных hotfix-релизов.
- [FIX] В проекте есть предыдущий фикс замены текста `Отправть` на `Отправить` в другом шаблоне, выполненный без побочных эффектов.

## Сопоставление и расхождения
- Проектный файл и ручная проверка совпадают: опечатка действительно есть в одном шаблоне.
- Документация требует корректный русский текст, а фактическая реализация нарушает это правило.
- Архитектурных конфликтов, изменений схем данных и миграций не требуется.

## Вывод
Локальная правка допустима как small-fix в ручном режиме: достаточно изменить строку в `src/ui/notify_form.html`, проверить экран формы и обновить краткую запись в документации.
