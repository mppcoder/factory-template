# Пример хорошо заполненного реестра доказательств

## Записи
- [PROJECT] В шаблоне уже есть `.chatgpt/task-index.yaml`, `stage-state.yaml` и `scenario-pack/`. Источник: текущее дерево проекта. Почему важно: новые примеры должны встроиться в существующий pipeline, а не заменить его.
- [PROJECT] В `template-repo/template/.chatgpt/` уже лежат пустые шаблоны для `user-spec`, `tech-spec`, `reality-check` и `evidence-register`. Источник: текущее состояние репозитория. Почему важно: можно усиливать именно содержимое, не меняя структуру.
- [FIX] Ранее было зафиксировано требование держать core универсальным и не делать brownfield-ядро зависимым от domain reference. Источник: принятые решения по фабрике. Почему важно: новые примеры должны быть универсальными.
- [DOC] Внутренний стандарт фабрики требует, чтобы Codex получал только нормализованные артефакты. Источник: сценарии handoff и stage-gates. Почему важно: примерные файлы должны помогать нормализации, а не подменять её.
- [REAL] В проектной работе repeatedly возникала проблема: пустые шаблоны описывают форму, но не показывают ожидаемую глубину заполнения. Источник: накопленный опыт использования фабрики. Почему важно: примеры действительно решают практическую проблему старта.
- [ASSUMPTION] Пользователю будет проще запускать фабрику, если примерные файлы лежат рядом с пустыми шаблонами. Источник: вывод из наблюдений, требует подтверждения на реальном использовании. Почему важно: эту гипотезу стоит проверить после нескольких запусков.
