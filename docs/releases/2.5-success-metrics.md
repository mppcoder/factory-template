# Метрики успеха release 2.5

## Scope / область

Эта таблица задаёт единые KPI для двух треков `2.5-A` и `2.5-B`.
Метрики обязательны для gate-проверок в `docs/releases/2.5-roadmap.md`.
Release truth source: `docs/releases/release-scorecard.yaml`.
Current scorecard result: GA passed: all M25 metrics meet full 2.5 thresholds with repository evidence.

## Таблица метрик

| Metric ID | Метрика | Зачем нужна | MVP-порог | Full 2.5 порог | Primary evidence |
|---|---|---|---|---|---|
| `M25-01` | Time-to-first-success для beginner path | Показывает реальный порог входа | <= `45` минут | <= `25` минут | scripted onboarding run + verify logs |
| `M25-02` | First-run completion rate для beginner path | Проверяет UI-friendly и ясность шагов | >= `80%` | >= `92%` | controlled pilot checklist |
| `M25-03` | Manual intervention count на первый запуск | Чем меньше, тем проще путь для новичка | <= `3` | <= `1` | run notes + support prompts register |
| `M25-04` | Downstream safe-sync success rate | Защищает battle repos от regressions | >= `95%` | >= `99%` | downstream sync verification reports |
| `M25-05` | Critical defects в scope `2.5` | Контроль инженерного качества | `0` открытых критических | `0` открытых критических | `reports/bugs/*.md` + verification summary |
| `M25-06` | Handoff rework loops на задачу | Показатель качества framing и routing | <= `2` | <= `1` | `.chatgpt` handoff history |
| `M25-07` | Release-facing doc consistency | Защищает от расхождения docs vs execution | 100% обязательных документов синхронизированы | 100% + без follow-up drift | checklist + review pass |
| `M25-08` | Repo-first routing compliance | Гарантирует соблюдение master-router first step | 100% | 100% | task evidence + review logs |

## Notes по измерениям

- Метрики `M25-01`..`M25-03` относятся в основном к треку `2.5-B`.
- Метрики `M25-04`..`M25-06` относятся в основном к треку `2.5-A`.
- Метрики `M25-07` и `M25-08` являются сквозными и обязательны для обоих треков.
- Если хотя бы одна метрика ниже порога, соответствующий gate `G25-*` считается не закрытым.

## Mapping gates / связь с gate'ами

1. `G25-RC` требует зеленой RC evidence baseline: consolidated verify, novice onboarding smoke, downstream upgrade/rollback UX.
2. `G25-GA` требует все `M25-*` на full-порогах и явный release decision.
3. Если `docs/releases/release-scorecard.yaml` говорит `ga_ready: false`, markdown-отчеты не должны объявлять GA-ready статус.

## Статус GA-проверки

На closeout `2026-04-26` gate `G25-GA` прошел после добавления измеримых evidence-артефактов:

| Metric ID | GA status | Evidence |
|---|---|---|
| `M25-01` | pass | `tests/onboarding-smoke/ACCEPTANCE_REPORT.md` фиксирует max time-to-first-success ниже `25` минут |
| `M25-02` | pass | `reports/release/2.5-controlled-pilot-checklist.md` фиксирует `9/9` controlled pilot scenarios, `100%` completion |
| `M25-03` | pass | `tests/onboarding-smoke/ACCEPTANCE_REPORT.md` фиксирует `0` manual interventions |
| `M25-04` | pass | `reports/release/2.5-downstream-safe-sync-report.md` фиксирует `100%` safe-sync success rate |
| `M25-05` | pass | `reports/bugs/*.md` и verify summary не фиксируют открытых critical defects |
| `M25-06` | pass | `.chatgpt/handoff-rework-register.yaml` фиксирует max `1` rework loop |
| `M25-07` | pass | release-facing docs синхронизированы с scorecard |
| `M25-08` | pass | текущий task начат с чтения `template-repo/scenario-pack/00-master-router.md` |

Consolidated evidence: `docs/releases/2.5-ga-kpi-evidence.md`.

## Граница field evidence

`G25-GA` закрыт как repo-controlled gate: evidence выше подтверждает воспроизводимые проверки внутри `factory-template`, controlled novice scenarios, validators и synthetic downstream safe-sync checks.

Полевой proof на реальных проектах пока не заявлен как пройденный:

| Field contour | Status | Evidence artifact |
|---|---|---|
| battle greenfield project | passed: `mppcoder/greenfield-test` commit `cca68d5` | `reports/release/field-pilot-scenarios/01-greenfield-battle-project.md` |
| battle brownfield without repo | passed: `mppcoder/openclaw-brownfield` commit `7b3d1a4` | `reports/release/field-pilot-scenarios/02-brownfield-without-repo.md` |
| battle brownfield with repo | pending | `reports/release/field-pilot-scenarios/03-brownfield-with-repo.md` |
| downstream sync cycle 1 | pending | `reports/release/field-pilot-scenarios/04-downstream-sync-cycle-1.md` |
| downstream sync cycle 2 | pending | `reports/release/field-pilot-scenarios/05-downstream-sync-cycle-2.md` |

Полевой follow-up ведётся отдельно в `docs/releases/2.5.1-field-pilot-roadmap.md` и `reports/release/2.5-field-pilot-evidence.md`. На 2026-04-26 выполнены `FP-01` и `FP-02` на GitHub-backed project repos; общий статус field evidence: `partial-field-evidence`, `2/5`.
