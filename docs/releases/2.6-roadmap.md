# Дорожная карта 2.6: deeper execution / runtime proof

## Назначение

`2.6` - следующая feature/execution линия после stabilization `2.5.1`.

Этот roadmap планирует более глубокую production automation и evaluation work. Он не объявляет `2.6` release-ready и не меняет `2.5.0` scorecard.

## Этап pipeline

- Current stage: `release-followup -> planning`
- Current status: `planned, not release-ready`; P3-S3/P3-S4 repo-local eval/execution evidence and P3-S5 runtime QA boundary preparation are complete, real runtime proof remains pending.
- Depends on: `docs/releases/2.5.1-roadmap.md`
- Gap register: `docs/releases/post-2.5-gap-register.md`

## Область 2.6

| ID | Item | Type | Target artifact | Gate |
|---|---|---|---|---|
| `260-RUNTIME-01` | Execute real production VPS field deploy on approved infrastructure. | external runtime proof | `docs/production-vps-field-pilot.md`, sanitized runtime transcript, sanitized release report | Explicit user approval, VPS access, secrets entered outside repo, pre-deploy QA passed, successful post-deploy healthcheck transcript. |
| `260-RUNTIME-02` | Execute backup restore test into disposable/staging target. | runtime proof | sanitized restore transcript, updated field pilot report | Fresh backup exists, restore target is non-production or explicitly approved, result recorded without secrets. |
| `260-RUNTIME-03` | Execute rollback drill for image/tag and document DB rollback boundary. | runtime proof | sanitized rollback transcript, operator notes | Previous image/tag recorded, rollback performed, healthcheck green, DB rollback limitations explicit. |
| `260-EVAL-01` | Expand Artifact Eval Harness beyond samples. | advanced eval | `docs/artifact-eval-harness.md`, `tests/artifact-eval/specs/*`, reports | Completed for P3-S3: routing-critical artifacts have deterministic specs and validator-backed reports. |
| `260-EXEC-01` | Adopt `feature-execution-lite` on real factory work, not only fixtures. | feature execution | `docs/feature-execution-lite.md`, `work/completed/plan-3-eval-adoption`, done reports | Completed for P3-S4: one real feature uses waves/checkpoint/decisions/final verification and closes through Project Knowledge Done Loop. |
| `260-HYGIENE-01` | Runtime/source hygiene backlog. | hardening | gap register, production VPS report, source-pack/export docs | Backlog items classified as internal repo, external runtime, downstream sync, sanitized transcript or manual/secrets boundary. |
| `260-SYNC-01` | Broaden optional downstream sync adoption after `2.5.1`. | field hardening | downstream sync reports | Multiple downstream lineages complete controlled sync or record blockers without project-owned overwrite. |

## Gate'ы

- `G260-0`: `2.5.1` stabilization artifacts are current and verify green.
- `G260-1`: real VPS work has explicit approval and no secrets are committed.
- `G260-2`: production proof separates pre-deploy QA, deploy, post-deploy healthcheck, backup restore, rollback and sanitized transcript evidence.
- `G260-3`: Artifact Eval Harness catches at least one meaningful negative fixture for expanded artifact classes. Status: satisfied for P3-S3 repo-local evidence.
- `G260-4`: `feature-execution-lite` adoption produces decisions, checkpoint, final verification and Project Knowledge update proposal. Status: satisfied for P3-S4 repo-local evidence.
- `G260-5`: release-facing docs state remaining field gaps without claiming release readiness.

## Идеи из `pavel-molyanov/molyanov-ai-dev`

Reference snapshot: `pavel-molyanov/molyanov-ai-dev` HEAD `1bfe745d43b0602d8279cb4c9cb7894b1bb56bba`.

| Category | Status in factory-template | 2.6 decision |
|---|---|---|
| Project Knowledge as just-in-time context | already adapted | Keep factory-native `project-knowledge/` and Done Loop; do not import `.claude` layout. |
| User spec -> tech spec -> tasks pipeline | already adapted | Continue through factory `work/` templates, spec traceability and validators. |
| Wave-based feature execution with checkpoint/resume | adapted for one real factory feature | Keep `feature-execution-lite` optional and use it when scope/risk justifies advanced execution. |
| Multi-validator planning/review | partially adapted | Keep lightweight validators in quick path; add targeted artifact eval/review where risk justifies it. |
| Skill testing with trigger-positive/negative evals | adapted for expanded routing-critical evals | Keep expanding Artifact Eval Harness only where deterministic repo artifacts need durable evidence. |
| Pre-deploy / post-deploy QA split | useful, not fully adapted | Use for production VPS field proof and runtime release gates. |
| Infrastructure migration backlog | useful, not fully adapted | Fold into runtime/source-hygiene backlog with factory boundary taxonomy. |
| Claude-specific commands, TeamCreate and `.claude` operational model | intentionally not adapted | Preserve Codex/repo-first routing and avoid importing tool-specific execution assumptions. |
| Automatic parallel agents for every task | intentionally not adapted as default | Keep optional advanced execution; beginner path remains guided and conservative. |

## Внешняя граница

The main remaining `2.6` blockers are outside repo execution: production VPS access, secrets, deploy approval, restore target approval and runtime transcripts. Repo docs can prepare and validate the path, but cannot turn dry-run evidence into production proof.
