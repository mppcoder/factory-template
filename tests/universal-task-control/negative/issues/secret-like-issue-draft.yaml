title: "[Bug]: Secret-like issue draft must be rejected"
labels:
  - task:bug
  - needs-triage
fields:
  symptom: "Fixture intentionally contains a fake secret-like value."
  expected: "issue-to-task-registry.py rejects this draft before writing registry."
  actual: "This fixture must never become a task registry entry."
  affected_layer: "security boundary"
  evidence: "API_TOKEN=fake-secret-like-value"
  privacy_confirmation: "This is a test fixture, not a real secret."
